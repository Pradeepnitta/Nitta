{
  "name": "nitta-monorepo",
  "version": "1.0.0",
  "description": "Nitta monorepo containing Express backend and Vite React frontend, served under a single link.",
  "scripts": {
    "install-backend": "cd backend && npx --yes cross-env SKIP_FRONTEND_BUILD=true npm install",
    "install-frontend": "cd frontend/vite-project && npm install",
    "build-frontend": "cd frontend/vite-project && npm run build",
    "build": "npm run install-backend && npm run install-frontend && npm run build-frontend",
    "postinstall": "npm run build",
    "start": "cd backend && npm start"
  }
}
